<nav class="navbar navbar-expand-md navbar-sharetrips sticky-top">
    <div class="container">

        <a class="navbar-brand-st" href="{{ path('app_home') }}">
            Share<span>Trips</span>
        </a>

        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarMain">
            <ul class="navbar-nav me-auto mb-2 mb-md-0 gap-1 ms-3">
                <li class="nav-item">
                    <a class="nav-link-st {{ app.request.attributes.get('_route') starts with 'app_trip' ? 'active' }}"
                       href="{{ path('app_trips') }}">
                        <i class="bi bi-map"></i> Trajets
                    </a>
                </li>
                {% if app.user %}
                    <li class="nav-item">
                        <a class="btn-propose" href="{{ path('app_profile_trip_form') }}">
                            <i class="bi bi-plus-circle"></i> Proposer un trajet
                        </a>
                    </li>
                {% endif %}
            </ul>

            <ul class="navbar-nav ms-auto mb-2 mb-md-0 align-items-md-center gap-2">
                {% if app.user %}
                    <li class="nav-item">
                        {% set unread = unread_messages_count() %}
                        <a class="nav-link-st position-relative"
                            href="{{ path('app_conversation_list') }}"
                            style="display:flex;align-items:center;gap:.4rem;">
                            <i class="bi bi-chat-dots"></i>
                            <span class="d-none d-md-inline">Messages</span>
                            {% if unread > 0 %}
                            <span id="unread-badge"
                                    style="
                                    position:absolute;
                                    top:-4px;right:-6px;
                                    background:#dc2626;
                                    color:#fff;
                                    font-size:.65rem;
                                    font-weight:700;
                                    min-width:18px;height:18px;
                                    border-radius:9px;
                                    display:flex;align-items:center;justify-content:center;
                                    padding:0 4px;
                                    line-height:1;
                                    box-shadow:0 0 0 2px var(--bs-body-bg, #fff);
                                    ">
                                {{ unread > 9 ? '9+' : unread }}
                            </span>
                            {% else %}
                            <span id="unread-badge" style="display:none;
                                    position:absolute;top:-4px;right:-6px;
                                    background:#dc2626;color:#fff;font-size:.65rem;font-weight:700;
                                    min-width:18px;height:18px;border-radius:9px;
                                    display:none;align-items:center;justify-content:center;
                                    padding:0 4px;line-height:1;
                                    box-shadow:0 0 0 2px var(--bs-body-bg, #fff);">
                            </span>
                            {% endif %}
                        </a>
                    </li>

                    <li class="nav-item dropdown">
                        <a class="nav-link-st dropdown-toggle"
                           href="#" role="button" data-bs-toggle="dropdown">
                            <div class="avatar-st">
                                {{ app.user.firstName ? app.user.firstName|first|upper : '?' }}
                            </div>
                            <span>{{ app.user.firstName ?? 'Mon compte' }}</span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end dropdown-menu-st">
                            <li>
                                <a class="dropdown-item-st" href="{{ path('app_profile') }}">
                                    <i class="bi bi-person"></i> Mon profil
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item-st" href="{{ path('app_profile_vehicle') }}">
                                    <i class="bi bi-car-front"></i> Mes véhicules
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item-st" href="{{ path('app_profile_trip') }}">
                                    <i class="bi bi-map"></i> Mes trajets
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item-st" href="{{ path('app_profile_booking') }}">
                                    <i class="bi bi-ticket-perforated"></i> Mes réservations
                                </a>
                            </li>
                            <li><div class="dropdown-divider-st"></div></li>
                            <li>
                                <a class="dropdown-item-st danger" href="{{ path('app_logout') }}">
                                    <i class="bi bi-box-arrow-right"></i> Déconnexion
                                </a>
                            </li>
                        </ul>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="btn-login-st" href="{{ path('app_login') }}">
                            <i class="bi bi-box-arrow-in-right" style="color:#1d4ed8;"></i> Se connecter
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="btn-register-st" href="{{ path('app_register') }}">
                            S'inscrire
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>

    </div>
</nav>