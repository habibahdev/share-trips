<nav class="navbar navbar-expand-md bg-white sticky-top shadow-sm">
    <div class="container">

        <a class="navbar-brand" href="{{ path('app_home') }}">
            ShareTrips
        </a>

        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarMain">
            <ul class="navbar-nav me-auto mb-2 mb-md-0">
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') starts with 'app_trip' ? 'active' }}"
                        href="#">
                        🗺️ Trajets
                    </a>
                </li>
            </ul>

            <ul class="navbar-nav ms-auto mb-2 mb-md-0 align-items-md-center">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link {{ app.request.attributes.get('_route') starts with 'app_booking' ? 'active' }}"
                            href="{{ path('app_profile') }}">
                            👤 {{ app.user.firstName }}
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link rounded-pill px-3" href="{{ path('app_login') }}">
                            <i class="ri-logout-box-line me-1"></i> Déconnexion
                        </a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link rounded-pill px-3" href="{{ path('app_login') }}">
                            <i class="ri-login-box-line me-1"></i></i> Se connecter
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-primary fw-semibold btn-sm ms-2 rounded-pill px-3" href="{{ path('app_register') }}">
                            S'inscrire
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>

    </div>
</nav>