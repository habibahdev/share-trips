{% extends 'base.html.twig' %}

{% block title %}Inscription - ShareTrips{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6">

            <div class="card shadow-sm border-0">
                <div class="card-body p-4">

                    <h1 class="h3 fw-bold text-center mb-4">Créer un compte</h1>

                    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label class="form-label fw-medium">{{ form_label(form.firstName) }}</label>
                                {{ form_widget(form.firstName, {'attr': {'class': 'form-control' ~ (form.firstName.vars.errors|length ? ' is-invalid' : '')}}) }}
                                {{ form_errors(form.firstName) }}
                            </div>
                            <div class="col-md-6 mb-3">
                                <label class="form-label fw-medium">{{ form_label(form.lastName) }}</label>
                                {{ form_widget(form.lastName, {'attr': {'class': 'form-control' ~ (form.lastName.vars.errors|length ? ' is-invalid' : '')}}) }}
                                {{ form_errors(form.lastName) }}
                            </div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-medium">{{ form_label(form.email) }}</label>
                            {{ form_widget(form.email, {'attr': {'class': 'form-control' ~ (form.email.vars.errors|length ? ' is-invalid' : '')}}) }}
                            {{ form_errors(form.email) }}
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-medium">{{ form_label(form.phone) }}</label>
                            {{ form_widget(form.phone, {'attr': {'class': 'form-control' ~ (form.phone.vars.errors|length ? ' is-invalid' : '')}}) }}
                            {{ form_errors(form.phone) }}
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-medium">{{ form_label(form.plainPassword.first) }}</label>
                            {{ form_widget(form.plainPassword.first, {'attr': {'class': 'form-control' ~ (form.plainPassword.vars.errors|length ? ' is-invalid' : '')}}) }}
                        </div>

                        <div class="mb-4">
                            <label class="form-label fw-medium">{{ form_label(form.plainPassword.second) }}</label>
                            {{ form_widget(form.plainPassword.second, {'attr': {'class': 'form-control' ~ (form.plainPassword.vars.errors|length ? ' is-invalid' : '')}}) }}
                            {{ form_errors(form.plainPassword) }}
                        </div>

                        <button type="submit" class="btn btn-primary w-100">S'inscrire</button>

                    {{ form_end(form) }}

                </div>
            </div>

            <p class="text-center mt-3 text-muted small">
                Vous avez déjà un compte ?
                <a href="{{ path('app_login') }}" class="text-decoration-none">Se connecter</a>
            </p>

        </div>
    </div>
</div>
{% endblock %}